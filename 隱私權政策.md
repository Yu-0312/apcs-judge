# 隱私權政策 Privacy Policy

最後更新：2026 年 7 月 17 日

本政策說明「互動程式教室 × APCS 練習」（以下稱「本站」，網址 https://yu-0312.github.io/apcs-judge/ ）如何處理你的資料。本站為個人開發、非營利的靜態網站，沒有自營的使用者帳號後端；多數功能在你的瀏覽器或裝置本地完成。你主動使用 AI、線上執行或公共聊天室時，相關資料仍會傳送至下列第三方服務，聊天室管理者也可能為維運或處理檢舉而接觸公開訊息。以下逐項說明。

## 一、儲存在你裝置本地的資料（不會傳給本站）

本站大量使用瀏覽器的 `localStorage` 將你的使用狀態存在**你自己的裝置**上。這些資料不會傳輸到本站或開發者手上，只有你的瀏覽器讀得到。包含：

- **學習與作答紀錄**：教學完成進度、公開範例檢查結果、累計判讀、程式草稿、讀書計畫、介面語言與版面偏好。
- **錯題本**：程式判讀答錯、實作題未全過、以及 AI 解題上傳的題目，會整理存放在本地錯題本。
- **AI 相關**：選用模型可以保存在本機；Gemini API 金鑰與 AI 助教對話只放在目前分頁的 `sessionStorage`／記憶體，關閉分頁後失效。舊版曾存入 `localStorage` 的金鑰會在載入新版時移除。
- **AI 題目縮圖**：只有你勾選同意時，重編碼後的縮圖才會隨解題紀錄存入本機錯題本；預設不保存圖片。
- **聊天室暱稱**：你在聊天室自訂的暱稱。

你可以使用站內備份／清除功能，或透過瀏覽器的「清除網站資料」移除以上內容；清除後無法復原。即使金鑰只保留到分頁關閉，公用或他人可存取的電腦仍不適合輸入個人 API 金鑰。

## 二、會傳輸到第三方服務的資料

本站部分功能需要把資料送到第三方服務才能運作。這些傳輸是由你的瀏覽器**直接**送往各服務，本站不居中蒐集或留存。

### 1. AI 功能（Google Gemini）

當你使用「AI 貼題／拍照解題」或「AI 助教」時，貼上的題目、你的補充／教練模式回答與對話內容會連同你自備的 API 金鑰，直接從瀏覽器送往 Google Gemini API。題目圖片會先在本機限制尺寸、縮小並重編碼為 JPEG，以移除 EXIF／GPS 等原檔中繼資料；只有在你勾選傳送提示並按下讀題後才會送出。讀題完成後，解題或教練階段只使用你確認過的文字，不再重送圖片。若不希望資料送往 Google，請勿使用 AI 功能。

### 2. 線上判題（Judge0）

當你在實作題或互動教學中執行 C、C++ 或 Java 時，你撰寫的**程式碼與測試輸入**會送往公用判題服務 Judge0（ce.judge0.com）執行，並回傳結果。這是公開的第三方服務，請勿在程式碼或輸入中放入任何個人隱私或機密資訊。Python 通常改由瀏覽器內的 Pyodide Worker 執行，該情況程式碼不會送往 Judge0。

### 3. 聊天室（Firebase Realtime Database）— 請特別留意

啟用公共聊天室後，Firebase Anonymous Authentication 會為目前瀏覽器建立匿名識別碼；你送出的**暱稱、訊息文字、匿名 UID、隨機客戶端識別碼與伺服器時間戳記**會寫入 Firebase Realtime Database，並可被其他已登入的聊天室訪客讀取。這是本站**唯一會對外持續儲存、且其他使用者看得到**的資料。請注意：

- 這是一個**公開共用的聊天室**，你送出的訊息任何造訪者都看得到。
- 目前尚未承諾固定保存期限，介面也不保證提供自助刪除單則訊息；站方部署前應另行設定保留與管理流程。
- **請勿在聊天室輸入真實姓名、聯絡方式、金鑰或任何你不希望公開的資訊。**
- 若需要移除特定訊息，請透過下方聯絡方式提出。

### 4. 內容傳遞網路與程式庫（CDN）

本站的程式庫與資源由公用 CDN 載入，包括 cdnjs（Cloudflare）、jsDelivr、以及 Google（gstatic.com，供 Firebase 使用）。載入資源時，這些 CDN 依其技術運作會收到你的 IP 位址與瀏覽器基本資訊，這是所有使用 CDN 的網站共通的情形，受各 CDN 業者的隱私政策約束。

## 三、主機託管（GitHub Pages）

本站以 GitHub Pages 靜態託管。你造訪網站時，GitHub 可能依其服務運作記錄標準的伺服器存取日誌（例如 IP 位址、瀏覽器類型），此部分受 **GitHub 的隱私聲明**約束，本站無法存取這些日誌。

## 四、Cookie 與追蹤

本站**不使用** Cookie，也**未安裝** Google Analytics 或任何第三方廣告、行為追蹤工具。本站不會為了廣告目的側寫你的行為，也不販售任何資料。前述 `localStorage` 僅用於保存你自己的使用狀態，不用於跨站追蹤。

## 五、桌面小工具（Scriptable Widget）

「每日一題」iPhone／Mac 小工具透過 Scriptable 執行。它會從 GitHub 抓取題庫，並把你的**作答紀錄、連續天數與本地錯題**存在你**裝置本地的檔案**中，不會上傳。它同樣會用到上述 Judge0／Gemini 等服務時，適用本政策第二節的說明。

## 六、兒童與未成年人

本站主要面向準備 APCS 的學生族群，可能包含未成年人。本站不主動蒐集任何可識別個人身分的資料；使用 AI 與聊天室等會對外傳輸的功能時，請未成年使用者在師長或家長協助下，注意不要輸入個人資訊。

## 七、你的控制權

由於資料主要存於你的裝置，你握有主要控制權：可匯出備份、清除站內學習資料；可選擇不使用 AI、Judge0 或聊天室等會外傳的功能；可隨時清除目前工作階段的 API 金鑰。對於已送往第三方（Google、Judge0）或已寫入公開聊天室的資料，其後續處理受各該服務的政策約束，必要時可透過下方聯絡方式請求協助。

## 八、政策修改

本政策可能隨功能調整而更新，修改後即於本頁面生效並更新上方日期。重大變更會盡量在網站或專案中提示。

## 九、聯絡方式

對本政策有任何疑問，或需要協助移除公開聊天室中的特定訊息，請聯絡：

- 電子郵件：wang.yuchi.312@gmail.com
- GitHub Issues：https://github.com/Yu-0312/apcs-judge/issues
